For your NLP project to extract ESG information from PDFs, the typical workflow would involve the following steps:

OCR (Optical Character Recognition): If the PDF is not already in a text format (i.e., it is a scanned document or an image-based PDF), you need to perform OCR to extract the text from the PDF.
Preprocessing: Once you have the text, you can then proceed with text-cleaning, tokenization, and formatting.
Workflow
OCR: Convert the PDF to text using OCR if necessary.
Preprocessing: Clean and preprocess the extracted text.
Detailed Steps
OCR: Use OCR to extract text from the PDF.

If the PDF is image-based, use OCR to convert it to text.
If the PDF is text-based, you can directly extract the text without OCR.
Preprocessing:

Text Cleaning: Remove unwanted characters, normalize whitespace, and handle special characters.
Tokenization: Split the text into tokens (words, sentences, etc.).
Formatting: Format the text as needed for further analysis.